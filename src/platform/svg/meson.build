add_project_arguments('-DBUILD_SVG', language : 'cpp')

platform_src += ['platform/svg/graphic_svg.cpp']

if install_headerfiles
	install_headers([
		'graphic_svg.h'
	], subdir: 'clatexmath/platform/svg')
endif
